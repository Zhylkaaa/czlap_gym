from setuptools import setup

setup(
    name='czlap_the_robot',
    version='0.0.1',
    install_requeres=['gym',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'torch',
                      'pyyaml']
)